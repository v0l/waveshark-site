{
  "name": "waveshark-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit",
    "deploy": "vite build && bunx wrangler pages deploy dist --project-name waveshark --branch master"
  },
  "dependencies": {
    "preact": "^10.27.2",
    "preact-iso": "^2.10.1"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.2",
    "typescript": "^5.9.2",
    "vite": "^7.1.5"
  }
}
